import { expect } from "chai";
import { ethers } from "hardhat";

describe("AgreementsProtocol", function () {
  // Add your tests here
  it("Should deploy successfully", async function () {
    // Example test
    // const AgreementsProtocol = await ethers.getContractFactory("AgreementsProtocol");
    // const protocol = await AgreementsProtocol.deploy();
    // await protocol.waitForDeployment();
    // expect(await protocol.getAddress()).to.be.properAddress;
  });
});
